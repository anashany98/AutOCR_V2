# Samples

The `make demo` target generates synthetic examples (a PDF with a simple table and an invoice-style image) in this directory and runs the processing pipeline against them.

You can add your own test documents here; they will not be committed automatically.
